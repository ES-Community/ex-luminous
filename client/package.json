{
  "private": true,
  "name": "client",
  "version": "0.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "lobby": "electron . lobby",
    "package": "electron-packager . --out=dist --asar --overwrite --all",
    "postinstall": "npm rebuild --target=5.0.0 --runtime=electron --dist-url=https://atom.io/download/electron"
  },
  "devDependencies": {
    "@types/node": "^12.7.11",
    "@types/three": "^0.103.2",
    "electron": "^5.0.11",
    "electron-packager": "^14.0.6"
  },
  "dependencies": {
    "@grpc/proto-loader": "^0.5.2",
    "grpc": "^1.24.0",
    "three": "^0.109.0"
  }
}
